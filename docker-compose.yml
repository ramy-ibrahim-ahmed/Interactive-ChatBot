version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: onyx-guide
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --requirepass 36951

volumes:
  redis-data:
